if SHIELD_SPLIT_LEFT

config ZMK_KEYBOARD_NAME
	default "split"

config ZMK_SPLIT_BLE_ROLE_CENTRAL
	default y

endif

if SHIELD_SPLIT_RIGHT

config ZMK_KEYBOARD_NAME
	default "Split Right"
	
endif

if SHIELD_SPLIT_LEFT || SHIELD_SPLIT_RIGHT

config ZMK_SPLIT
	default y

endif
