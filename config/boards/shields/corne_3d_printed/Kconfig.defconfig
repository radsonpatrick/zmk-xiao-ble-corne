
if SHIELD_SPLIT_LEFT

config ZMK_KEYBOARD_NAME
	default "corner_printed"

config ZMK_SPLIT_BLE_ROLE_CENTRAL
	default y

endif

if SHIELD_SPLIT_RIGHT

config ZMK_KEYBOARD_NAME
	default "corne_3d_printed_R"
	
endif

if SHIELD_SPLIT_LEFT || SHIELD_SPLIT_RIGHT

config ZMK_SPLIT
	default y

endif
